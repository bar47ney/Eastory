<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StartActivities.MainActivity">

    <ImageView
        android:id="@+id/mainBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:src="@drawable/startbackground" />

    <LinearLayout
        android:id="@+id/startLayout"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:orientation="vertical">

        <Button
            android:id="@+id/startButton"
            android:layout_width="238dp"
            android:layout_height="wrap_content"
            android:background="@drawable/style_btn_start"
            android:text="@string/start_ru"
            android:textColor="@color/stred"
            android:textStyle="italic"
            android:textAllCaps="false"
            />


        <Button
            android:id="@+id/changeLanguage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/style_btn_start"
            android:text="@string/change_ru"
            android:textColor="@color/stred"
            android:textAllCaps="false"
            android:textStyle="italic"
            />
    </LinearLayout>

</RelativeLayout>